meta {
  name: Get Rentals
  type: http
  seq: 19
  tags: [admin]
}

get {
  url: {{baseUrl}}/admin/rentals
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("should list all rentals", function () {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}