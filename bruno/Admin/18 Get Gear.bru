meta {
  name: Get Gear
  type: http
  seq: 18
  tags: [admin]
}

get {
  url: {{baseUrl}}/admin/gear
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("should list all gear", function () {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}