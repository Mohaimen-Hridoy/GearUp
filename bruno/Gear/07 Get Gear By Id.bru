meta {
  name: Get Gear By Id
  type: http
  seq: 7
  tags: [gear, public]
}

get {
  url: {{baseUrl}}/gear/{{gearId}}
}

tests {
  test("should return gear details", function () {
    expect(res.status).to.equal(200);
    expect(res.body.data.id).to.equal(gearId);
  });
}