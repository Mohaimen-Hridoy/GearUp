meta {
  name: Get All Gear
  type: http
  seq: 8
  tags: [gear, public]
}

get {
  url: {{baseUrl}}/gear
}

tests {
  test("should list gear items", function () {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}